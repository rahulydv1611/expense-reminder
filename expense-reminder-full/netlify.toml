# netlify.toml — use correct base directory where your frontend lives
[build]
  base    = "expense-reminder-full/frontend"   # ← <- change this if needed
  command = "npm install && npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173
